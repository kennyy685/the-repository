# O4: King morning follow-ups (draft for FilthE to approve)

The King's scheduled routines only change with FilthE's approval on his MacBook (in Cowork). This is
the text to add to the **morning standup** routine. FilthE: open the routine on the Mac, paste the block
below at the end of its prompt, and approve.

Also replace the old line `Crew HQ board/current, re-synced from BOARD.md` with
`Crew HQ board/current is the source of truth; never overwrite it from BOARD.md.`

---

**Morning follow-ups (HMP App, Crew HQ database):**
1. Read `leads`, `claims` and `stats/week-<this ISO week>`.
2. Build today's list for FilthE (English) and the boss (Spanish), at most 8 items, most urgent first:
   - leads whose `next_step.due` is today or overdue (call-backs, estimates to send)
   - doors to revisit: leads with 1-2 visits and no answer, 2+ days since the last visit (3 passes total)
   - adjuster meetings today or tomorrow (`claims.adjuster_date`)
   - checks to chase: ACV not received 10+ days after the scope; mortgage checks out 7+ days;
     depreciation not requested after install
3. Write each as a follow-up on the lead or claim (`next_step` with `due` = today), so the Today tab shows it.
4. Post one `events` note from `king`: "Today: N follow-ups, M revisits, K checks to chase."
5. Never mention covering or waiving a deductible, or promise insurance will pay.
